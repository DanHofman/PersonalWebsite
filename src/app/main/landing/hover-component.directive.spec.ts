import { HoverComponentDirective } from './hover-component.directive';

describe('HoverComponentDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverComponentDirective();
    expect(directive).toBeTruthy();
  });
});
